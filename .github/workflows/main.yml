name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Step1 
      run: echo Publish site!
    - name: Execute Deploy
      run: |
        chmod +x ./deploy.sh
        ./deploy.sh
